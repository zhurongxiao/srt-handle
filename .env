# SRT Handle Configuration
# Default paths - modify these for different machines

# Default config file path for process and batch commands
SRT_HANDLE_CONFIG_PATH=/home/debian/rust/srt-handle/config.txt

# Default binary path for batch command (when calling srt-handle process)
SRT_HANDLE_BINARY_PATH=/home/debian/rust/srt-handle/target/release/srt-handle